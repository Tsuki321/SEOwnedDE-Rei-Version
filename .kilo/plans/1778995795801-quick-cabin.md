# Implementation Plan: Projectile Aimbot Prediction Fixes

**Derived from:** Audit Report (17 findings)
**Status:** Planning Complete — Pending Execution
**Strategy:** Phased implementation with verification at each phase boundary

---

## Dependency Graph
```
Phase 1 (Independent fixes) ─────────────────────────────┐
    Task 1a: dx guard            Task 1c: RAII scope      │
    Task 1b: k_flMaxVelocity     Task 1d: frozen state    │
                                                          │
Phase 2 (Convergence overhaul) ─── depends on Phase 1 ────┤
    Task 2a: Integrate PMath                              │
    Task 2b: Lerp compensation ─── depends on 2a ─────────┤
    Task 2c: Tolerance convergence ── depends on 2a,2b ───┤
                                                          │
Phase 3 (Movement fixes) ──────── depends on Phase 1 ─────┤
    Task 3a: Yaw wrapping                                 │
    Task 3b: Yaw rate formula    Task 3c: Time-aw are accel│
    Task 3d: Method 2 per-tick   Task 3e: Method 3 drift  │
                                                          │
Phase 4 (Physics) ─────────────── depends on Phase 2 ─────┤
    Task 4a: Drag refinement                              │
    Task 4b: Sim re-entrancy ──── depends on Phase 1,3 ───┤
                                                          │
Phase 5 (Aim refinement) ──────── depends on Phase 2 ─────┤
    Task 5a: BBOX param cleanup                           │
    Task 5b: Splash improvements                          │
                                                          │
Phase 6 (Minor cleanup) ───────── depends on Phase 3,4,5 ─┘
    Task 6a: Extract decomposition utility
    Task 6b: Unify air strafe records
    Task 6c: CanArcReach guard
    Task 6d: Sticky arm time
    Task 6e: HybridBlendFactor
```

---

## Phase 1: Foundation — Low-Risk, High-Impact Fixes

### Task 1a: Add dx=0 Division-by-Zero Guard
- **File:** `AimbotProjectile.cpp` — `CalcProjAngle`
- **Finding:** 14
- **Change:** Add guard at start of gravity path preventing division by zero when target is vertically aligned
- **Lines modified:** Insert after line 306 (the `if (g)` check), before line 316 (root calculation)
- **Risk:** Minimal — edge case safety check

### Task 1b: Apply k_flMaxVelocity Cap in GetProjectileInfo
- **File:** `AimbotProjectile.cpp` — `GetProjectileInfo`
- **Finding:** 4
- **Change:** Move the velocity cap into the speed assignment so `m_CurProjInfo.Speed` always reflects the capped value
- **Lines modified:** After the charge speed remap in the `TF_WEAPON_PIPEBOMBLAUNCHER` case (line 208)
- **Risk:** Minimal — removes silent discrepancy

### Task 1c: Add MovementSimulation RAII Scope Guard
- **Files:** `MovementSimulation.h`, `MovementSimulation.cpp`, `AimbotProjectile.cpp`
- **Finding:** 13
- **Change:** Create `CMovementSimScope` RAII class, replace manual Restore() calls in SolveTarget
- **Lines modified:** New class in header; replace lines 691-878 Restore() calls
- **Risk:** Medium — must verify all exit paths work with destructor-based restore

### Task 1d: Read Duck/Ground State Per-Tick
- **File:** `AimbotProjectile.cpp` — `SolveTarget`
- **Finding:** 16
- **Change:** Move `bDucked`/`bOnGround` extraction inside the simulation loop
- **Lines modified:** Move lines 688-689 inside the for loop at line 694
- **Risk:** Minimal — reading from in-simulation player state each tick

---

## Phase 2: Convergence & Latency Overhaul

### Task 2a: Integrate ProjectilePredictionMath Namespace
- **Files:** `AimbotProjectile.cpp`, `AimbotProjectilePrediction.h`
- **Finding:** 11
- **Change:** Add `#include "AimbotProjectilePrediction.h"` and call the scoring functions from the convergence loop
- **Key integration points:**
  - `ComputeTimingBias` — combine latency + lerp for tolerance window
  - `IsWithinTemporalTolerance` — replace integer equality check
  - `ComputeVelocityConfidence` — feed into Method 3 quality metric
- **Risk:** Medium — namespace functions are mathematically verified but untested in integration

### Task 2b: Add Client Interpolation (Lerp) to Latency Compensation
- **File:** `AimbotProjectile.cpp` — `SolveTarget`
- **Finding:** 2
- **Change:** Add `SDKUtils::GetLerp()` to the nTargetTick calculation
- **Lines modified:** Line 711 and 983
- **Depends on:** Task 2a (`ComputeTimingBias` provides the combined value)
- **Risk:** Low — one-line additive change to existing latency offset

### Task 2c: Replace Integer Convergence with Tolerance-Based Check
- **File:** `AimbotProjectile.cpp` — `SolveTarget`
- **Finding:** 1
- **Change:** Replace `nTargetTick == nTick || nTargetTick == nTick - 1` with `abs(nTargetTick - nTick) <= TOLERANCE_TICKS` using the scoring framework
- **Lines modified:** Line 735
- **Depends on:** Task 2a (provides `IsWithinTemporalTolerance`)
- **Risk:** High — core convergence logic; requires extensive verification

---

## Phase 3: Movement Simulation Fixes

### Task 3a: Fix Yaw Angle Wrapping
- **File:** `MovementSimulation.cpp` — `SetupMoveData`
- **Finding:** 5
- **Change:** Add helper `NormalizeYawDiff()` and wrap all yaw deltas before monotonicity check and rate calculation
- **Lines modified:** 352-373 (ground), 387-416 (air)
- **Risk:** Medium — affects strafe prediction for east-west sightlines

### Task 3b: Fix Ground Strafe Yaw Rate Formula
- **File:** `MovementSimulation.cpp` — `SetupMoveData`
- **Finding:** 6
- **Change:** Replace the numerator (add missing yaw1-yaw2 pair) and denominator (use actual record time span instead of `TICK_INTERVAL * 50`)
- **Lines modified:** Line 366
- **Risk:** Medium — formula change affects all ground strafe scenarios

### Task 3c: Incorporate Lag Record Timestamps in Acceleration Calculations
- **File:** `MovementSimulation.cpp` — `SetupMoveData`
- **Finding:** 8
- **Change:** Read `SimulationTime` from lag records and divide velocity deltas by actual Δt
- **Lines modified:** 166-200 (Method 2), 246-290 (Method 3 regression)
- **Risk:** Medium — requires `SimulationTime` field access from `LagRecord_t`

### Task 3d: Apply Method 2 Acceleration Per-Tick
- **File:** `MovementSimulation.cpp` — `RunTick`
- **Finding:** 7
- **Change:** Add per-tick acceleration updating block for Method 2 in RunTick (analogous to lines 534-567 for Method 3)
- **Lines modified:** Insert between lines 531-532
- **Risk:** Medium — new per-tick behavior, must not conflict with Method 3 path

### Task 3e: Add Directional Drift Limiter to Method 3
- **File:** `MovementSimulation.cpp` — `RunTick`
- **Finding:** 9
- **Change:** After the per-tick velocity update, compute angular change from original velocity; dampen or freeze if exceeding threshold
- **Lines modified:** After line 537, before line 540
- **Risk:** Medium — affects Method 3 prediction accuracy

---

## Phase 4: Physics & Solver

### Task 4a: Refactor Pipe Drag Model with Iterative Refinement
- **File:** `AimbotProjectile.cpp` — `CalcProjAngle`
- **Finding:** 3
- **Change:** Replace the two-pass magic-coefficient approach with fixed-point iteration (3-5 passes) using the parabolic solver and physics sim drag basis for correction
- **Lines modified:** 327-365
- **Depends on:** Phase 2 (uses improved convergence framework)
- **Risk:** High — changes the core ballistic solution for all pipe weapons

### Task 4b: Make ProjectileSim Re-entrant
- **File:** `ProjectileSim.cpp`, `ProjectileSim.h`
- **Finding:** 15
- **Change:** Move global `env`/`obj` into `CProjectileSim` member variables; create/destroy per simulation session
- **Lines modified:** 3-4 (remove globals), add members to class, modify Init/RunTick/GetOrigin
- **Risk:** Medium — must ensure proper cleanup and no memory leaks from physics objects

---

## Phase 5: Aim Refinement

### Task 5a: Pass Aim Position as Parameter
- **File:** `AimbotProjectile.cpp`, `AimbotProjectile.h`
- **Finding:** 10
- **Change:** Add `int aimPosition` parameter to `OffsetPlayerPosition`, remove global CFG dependency
- **Lines modified:** 411 (signature), 415, 849-855 (callers)
- **Depends on:** Phase 2 (avoids conflict with convergence changes)
- **Risk:** Low — refactoring, no behavior change

### Task 5b: Splash Sampling Improvements
- **File:** `AimbotProjectile.cpp` — `SolveTarget` (both lambdas)
- **Finding:** 12
- **Changes:**
  - Bias Fibonacci sampling toward shooter-facing hemisphere
  - Unify player/building splash radii (or document discrepancy)
  - Sort candidates by combined distance-to-target + travel-time metric
- **Lines modified:** 754, 764-766, 782-784, 901, 910-912, 930-932
- **Risk:** Low — sampling refinements, backwards-compatible

---

## Phase 6: Minor Cleanup

### Task 6a: Extract Velocity Decomposition Utility
- **File:** `MovementSimulation.cpp`, `MovementSimulation.h`
- **Finding:** 17a
- **Change:** Extract the Cramer's rule decomposition into `static bool DecomposeVelocity(const Vec3& velocity, const Vec3& viewAngles, float& forward, float& side)`
- **Lines modified:** Replace 3 copies with single function call
- **Depends on:** Phase 3 (after all movement changes stabilize)

### Task 6b: Unify Air Strafe Record Count to 5
- **File:** `MovementSimulation.cpp` — `SetupMoveData`
- **Finding:** 17b
- **Change:** Uncomment `rec4` in air strafe, adjust monotonicity check and delta formula to match ground strafe's 5-record window
- **Lines modified:** 383, 385, 391, 393, 401
- **Depends on:** Phase 3 (applied after yaw wrapping fix)

### Task 6c: Fix CanArcReach Default Return
- **File:** `AimbotProjectile.cpp` — `CanArcReach`
- **Finding:** 17c
- **Change:** Before returning true on timeout, verify the projectile would still be within max range via discriminant check
- **Lines modified:** Insert check before line 613
- **Risk:** Low — guarded fallback

### Task 6d: Direct Sticky Arm Time Constraint
- **File:** `AimbotProjectile.cpp` — `SolveTarget`
- **Finding:** 17d
- **Change:** Replace tick-fudging with `if (flTimeToTarget < sticky_arm_time) continue;` at convergence site
- **Lines modified:** 726-733
- **Risk:** Low — logic simplification, same outcome

### Task 6e: Fix HybridBlendFactor EndAlpha
- **File:** `AimbotProjectilePrediction.h` — `ComputeHybridBlendFactor`
- **Finding:** 17e
- **Change:** Make `endAlpha` a function of velocity confidence rather than fixed 0.15
- **Lines modified:** 63-64
- **Risk:** Low — affects unused function (Task 2a integration will call it)

---

## Execution Order Summary
1. Task 1a → 1b → 1c → 1d (Phase 1 — independent)
2. Task 2a → 2b → 2c (Phase 2 — sequential dependencies)
3. Task 3a → 3b → 3c → 3d → 3e (Phase 3 — can run in parallel with Phase 2)
4. Task 4a → 4b (Phase 4 — depends on Phase 2 + Phase 3)
5. Task 5a → 5b (Phase 5 — depends on Phase 2)
6. Task 6a → 6b → 6c → 6d → 6e (Phase 6 — depends on Phases 3,4,5)

## Verification Strategy
Each phase boundary requires:
- Compilation success
- No regression in existing behavior (Phase 1, 3, 5, 6 changes should preserve functionality)
- Improved behavior verified via the existing debug overlay visualization (`DrawMovePath`, `DrawProjPath`)

Begin execution with **Phase 1**.
